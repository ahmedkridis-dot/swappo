.DS_Store

# ── Node / Capacitor ────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*

# Capacitor build outputs (large native binaries — regenerate with cap sync)
ios/App/build/
ios/App/Pods/
ios/App/DerivedData/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/

# Keep capacitor.config.json + ios/ + android/ source folders in repo,
# only the build artifacts are excluded.

# IDE
.vscode/
.idea/
*.iml

# OS
Thumbs.db
